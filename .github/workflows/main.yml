name: ci

on:
  push:
    branches: main

jobs:
  login:
    runs-on: ubuntu-latest
    steps:
      -
        name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username:veerubind
          password:dckr_pat_rgjU682kx4tjGwqg1MkFl-hDA-Y
